name: 📦 Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      GRADLE_OPTS: "-Dorg.gradle.daemon=false"
      JAVA_VERSION: "17"
      KEY_ALIAS: "androiddebugkey"
      KEY_PASS: "android"
      STORE_PASS: "android"
      KEYSTORE_FILE: "android/app/auto-release.jks"

    steps:
      # 1️⃣ Клонируем проект
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Устанавливаем Flutter
      - name: 🧱 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'

      # 3️⃣ Генерируем тестовый keystore
      - name: 🔐 Generate keystore
        run: |
          keytool -genkeypair \
            -v \
            -keystore $KEYSTORE_FILE \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -alias $KEY_ALIAS \
            -storepass $STORE_PASS \
            -keypass $KEY_PASS \
            -dname "CN=GitHub, OU=CI, O=NoticeApp, L=Internet, S=Web, C=US"

      # 4️⃣ Устанавливаем зависимости
      - name: 📦 Install dependencies
        run: flutter pub get

      # 5️⃣ Очищаем проект
      - name: 🧹 Clean project
        run: flutter clean

      # 6️⃣ Создаем релизный APK
      - name: 🏗️ Build release APK
        run: flutter build apk --release

      # 7️⃣ Загружаем APK как артефакт
      - name: 📤 Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: notice_app_apk
          path: build/app/outputs/flutter-apk/app-release.apk
